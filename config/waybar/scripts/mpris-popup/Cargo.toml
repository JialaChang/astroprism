[package]
name = "mpris-popup"
version = "0.1.0"
edition = "2024"

[dependencies]
gdk = "0.18"
gdk-pixbuf = "0.18"
gio = "0.18"
glib = "0.18"
gtk = "0.18"
gtk-layer-shell = { version = "0.8.2", features = ["v0_6"] }
nix = { version = "0.31.3", features = ["signal", "process"] }
regex = "1.13.0"
ureq = "3.3.0"
