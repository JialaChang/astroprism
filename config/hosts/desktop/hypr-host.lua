-- Desktop-specific Hyprland values.
-- Deployed to ~/.config/hypr/host.lua by deploy.sh; see config/hosts/README.md.
return {
	gaps_out = 20,
	-- Extra processes to autostart on this host.
	autostart = {},
}
